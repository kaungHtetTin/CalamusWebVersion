# SPA fallback: serve index.html for client-side routes so refresh works
<IfModule mod_rewrite.c>
  RewriteEngine On
  # If the request is for a real file or directory, serve it
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  # Otherwise send everything to index.html (React Router handles the route)
  RewriteRule . index.html [L]
</IfModule>
